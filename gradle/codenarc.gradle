apply plugin: 'codenarc'

codenarcMain {
    ignoreFailures = false
    configFile = rootProject.file('codenarcrule.groovy')
}
codenarcTest {
    ignoreFailures = false
    configFile = rootProject.file('codenarcrule-test.groovy')
}